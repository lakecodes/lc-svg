import { ReturnKeysPipe } from './return-keys.pipe';

describe('ReturnKeysPipe', () => {
  it('create an instance', () => {
    const pipe = new ReturnKeysPipe();
    expect(pipe).toBeTruthy();
  });
});
